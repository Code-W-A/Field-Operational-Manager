rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Fără restricții — oricine poate citi/scrie orice document. NU folosi în producție.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
